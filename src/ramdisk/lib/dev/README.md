# ramdisk/lib/dev

Contains lower level device functionality, with a class interface
that calls OS specific libraries, that support the project.

May start using the psutil pip installed python library to assist
with this part of the project.

